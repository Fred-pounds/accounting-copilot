# Terraform Backend Configuration (Example)
# Rename this file to backend.tf and configure for your environment

# Option 1: S3 Backend (Recommended for team collaboration)
# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket"
#     key            = "accounting-copilot/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-state-lock"
#   }
# }

# Option 2: Local Backend (Default - for development)
# No configuration needed - Terraform uses local backend by default
